apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "wotlwedu-ui.fullname" . }}-nginx
  labels:
    {{- include "wotlwedu-ui.labels" . | nindent 4 }}
data:
  default.conf: |
{{ .Values.nginx.defaultServerConfig | indent 4 }}
